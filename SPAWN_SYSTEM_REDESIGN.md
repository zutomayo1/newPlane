# 敌人刷新系统重设计文档

## 问题分析

### 旧系统的不合理之处

1. **等概率生成** ❌
   - 所有已解锁敌人使用 `random.choice(pool)` 等概率选择
   - 结果：难敌人（如 vortex）与简单敌人（如 drone）出现率相同
   - 影响：游戏节奏突兀，难度跳跃

2. **生成率太低** ❌
   - 基础生成率：3% + 等级 × 0.2%
   - 第10级时：3.2%（几乎无增长）
   - 结果：敌人稀疏，游戏压力不足

3. **难度跳跃** ❌
   - 分数到达阈值时，敌人池突然扩展
   - 例如：2000分时，突然可以出现 tank/spike
   - 结果：玩家毫无准备

4. **缺乏过渡** ❌
   - 从简单到困难的敌人没有平滑过渡
   - 新敌人与旧敌人权重相同
   - 结果：教学性差，难度曲线不合理

---

## 新系统设计

### 核心改进

#### 1. **分阶段难度系统** ✅

游戏分为 5 个阶段，每阶段对应分数区间和敌人池：

| 阶段 | 分数范围 | 阶段名 | 主要敌人 | 难度 |
|------|---------|--------|---------|------|
| 0 | 0-2000 | 学习期 | drone, chaser | ⭐ |
| 1 | 2000-4000 | 过渡期 | +tank, spike | ⭐⭐ |
| 2 | 4000-6000 | 挑战期 | +wasp, sentinel | ⭐⭐⭐ |
| 3 | 6000-8000 | 激烈期 | +glitch, orbiter | ⭐⭐⭐⭐ |
| 4 | 8000+ | 绝望期 | +vortex | ⭐⭐⭐⭐⭐ |

#### 2. **加权概率系统** ✅

每个阶段敌人池中敌人有不同权重，控制出现概率：

```python
stage_pools = [
    # 阶段0: 学习期 - 简单敌人为主
    [
        ("drone", 40),        # 40% 概率
        ("chaser", 35),       # 35% 概率
        ("sniper", 15),       # 15% 概率
        ("phantom", 10),      # 10% 概率
    ],
    # ...（其他阶段类似）
]
```

**权重特点**：
- 早期阶段：简单敌人权重高（40-35）
- 过渡阶段：新敌人权重逐步升高
- 后期阶段：难敌人权重接近（15-18）

#### 3. **动态生成率调整** ✅

生成率从 3% 指数增长到 8%：

```python
base_spawn_rate = 0.03 + 0.05 * (1 - math.exp(-player.level / 20))
```

**数据示例**：
| 玩家等级 | 生成率 | 增长 |
|---------|--------|------|
| 1 | 3.2% | 基础 |
| 5 | 3.8% | +0.6% |
| 10 | 4.8% | +1.6% |
| 20 | 6.3% | +3.1% |
| 40 | 7.5% | +4.3% |
| ∞ | 8.0% | 上限 |

---

## 阶段性设计详解

### 阶段 0: 学习期 (0-2000分)

**目标**：教学、熟悉机制
- **敌人池**：drone, chaser, sniper, phantom
- **权重分布**：
  - drone: 40% (最常见，教学敌人)
  - chaser: 35% (移动敌人，教学追逐)
  - sniper: 15% (远程敌人，学习躲避)
  - phantom: 10% (传送敌人，少见挑战)

### 阶段 1: 过渡期 (2000-4000分)

**目标**：逐步提升难度
- **新增敌人**：tank, spike
- **权重调整**：
  - drone 下降至 20% (开始稀少)
  - chaser 维持 25% (仍为主力)
  - tank: 20% (新威胁)
  - spike: 18% (新威胁，出现率略低)
  - sniper/phantom: 保持学习期水平

### 阶段 2: 挑战期 (4000-6000分)

**目标**：真正的挑战开始
- **新增敌人**：wasp, sentinel
- **权重变化**：
  - drone 移除 (完全消失)
  - chaser 下降至 15%
  - tank: 20% (主力)
  - spike: 18% (常见)
  - wasp: 15% (轰炸机威胁)
  - sentinel: 12% (防御性威胁)
  - sniper/phantom: 各 12%/8% (减少)

### 阶段 3: 激烈期 (6000-8000分)

**目标**：高压环境，需要玩家高水平操作
- **新增敌人**：glitch, orbiter
- **权重分布**：
  - tank: 15% (仍有但减少)
  - wasp: 15% (持续威胁)
  - sentinel: 18% (提升为主力)
  - glitch: 12% (新的有机威胁)
  - orbiter: 15% (漂浮威胁)
  - phantom: 10% (传送威胁)
  - spike: 10% (持续存在)
  - sniper: 5% (几乎消失)

### 阶段 4: 绝望期 (8000分+)

**目标**：最高难度，混合所有威胁
- **新增敌人**：vortex
- **权重分布**：
  - wasp: 10% (仍存在但权重低)
  - sentinel: 15% (防御主力)
  - glitch: 15% (有机威胁)
  - orbiter: 15% (漂浮威胁)
  - vortex: 18% (最强威胁，权重最高！)
  - phantom: 12% (传送威胁)
  - spike: 12% (有机威胁)
  - tank: 7% (罕见出现)

---

## 算法对比

### 旧系统（不合理）
```python
# 问题1: 等概率
if score > 1000: pool.extend(["sniper", "phantom"])
if score > 2000: pool.extend(["tank", "spike"])
# ...
Enemy(random.choice(pool))  # 所有敌人等概率

# 问题2: 生成率低且增长缓慢
if random.random() < (0.03 + player.level * 0.002):
    # 第10级时: 3.2%, 第20级时: 3.4%
```

### 新系统（合理）
```python
# 改进1: 指数增长生成率
base_spawn_rate = 0.03 + 0.05 * (1 - math.exp(-player.level / 20))
# 第10级时: 4.8%, 第20级时: 6.3%

# 改进2: 加权选择
enemies = [e[0] for e in pool]
weights = [e[1] for e in pool]
chosen_type = random.choices(enemies, weights=weights, k=1)[0]

# 改进3: 阶段性池切换
stage = min(4, score // 2000)
pool = stage_pools[stage]
```

---

## 数据统计

### 分数到达方式示例

假设玩家平均每只敌人得分 20-100 分：

| 目标分数 | 对应阶段 | 需击杀敌人数 | 累计时间(分) |
|---------|---------|-----------|-----------|
| 0-2000 | 0 | 20-100 | 2-5 |
| 2000-4000 | 1 | 10-100 | 3-8 |
| 4000-6000 | 2 | 10-100 | 3-8 |
| 6000-8000 | 3 | 10-100 | 3-8 |
| 8000+ | 4 | ∞ | ∞ |

### 敌人出现次数统计（1000次敌人生成）

**阶段 0 中期**（1000分）：
- drone: 400 次
- chaser: 350 次
- sniper: 150 次
- phantom: 100 次

**阶段 2 中期**（5000分）：
- chaser: 150 次
- tank: 200 次
- spike: 180 次
- wasp: 150 次
- sentinel: 120 次
- sniper: 120 次
- phantom: 80 次

---

## 优势总结

✅ **合理的难度曲线**
- 平滑过渡，不会突兀跳跃
- 玩家有时间适应新敌人
- 进度感强

✅ **教学友好**
- 早期敌人简单，易于学习
- 逐步引入复杂机制
- 新玩家不会崩溃

✅ **可玩性提升**
- 生成率更激进，游戏压力充足
- 后期不会因为困难敌人过多而过度
- 难敌人权重合理控制

✅ **可扩展性好**
- 易于添加新敌人类型
- 易于调整难度参数
- 可根据玩家反馈快速迭代

---

## 参数调优建议

如果需要调整难度，修改以下部分：

### 1. 生成率
```python
# 如果敌人过多，降低系数
base_spawn_rate = 0.03 + 0.04 * (1 - math.exp(-player.level / 20))
# 如果敌人过少，提升系数
base_spawn_rate = 0.03 + 0.06 * (1 - math.exp(-player.level / 20))
```

### 2. 权重调整
```python
# 让 drone 在学习期更常见
("drone", 50),  # 从 40 提升至 50

# 让 vortex 在后期更常见
("vortex", 25),  # 从 18 提升至 25
```

### 3. 阶段分界
```python
# 阶段改为 2500 分为分界点
stage = min(4, score // 2500)

# 或改为 1500 分为分界点
stage = min(4, score // 1500)
```

---

## 实现细节

### 代码位置
- **文件**：`main.py`
- **行数**：约 1735-1785 行
- **依赖**：`math` 模块（已导入）

### 性能考虑
- 使用 `random.choices()` 替代 `random.choice()`
- 权重数组在循环外计算会更高效，但当前实现足够
- 无明显性能开销

---

## 总结

新的敌人刷新系统通过以下改进解决了旧系统的不合理之处：

1. **从等概率 → 加权概率**：敌人出现频率与难度匹配
2. **从低增长 → 指数增长**：生成率更合理
3. **从突跳 → 平滑过渡**：难度曲线自然流畅
4. **从无序 → 有序分阶段**：游戏节奏清晰

这使得游戏体验从"随意和突兀"转变为"可预测和循序渐进的"。
